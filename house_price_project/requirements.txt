# Note: keep versions flexible to support newer Python releases.
# If you need strict reproducibility, pin these in a separate lock file.
Django>=4.2
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
